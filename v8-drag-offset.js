(()=>{updateWeekDrag=(x,y)=>{const a=document.getElementById('weekBody'),r=a.getBoundingClientRect();if(drag.grabOffsetY==null){const q=drag.el.getBoundingClientRect();drag.grabOffsetX=x-q.left;drag.grabOffsetY=y-q.top}let di=Math.floor((x-r.left)/r.width*7);di=Math.max(0,Math.min(6,di));let m=(y-r.top-drag.grabOffsetY)/PPM;m=Math.round(m/15)*15;m=Math.max(0,Math.min(1439-drag.dur,m));drag.targetDate=DAYS[di][0];drag.targetMin=m;drag.ghost.style.left=Math.max(6,Math.min(innerWidth-drag.ghostW-6,x-drag.grabOffsetX))+'px';drag.ghost.style.top=Math.max(6,Math.min(innerHeight-drag.ghostH-6,y-drag.grabOffsetY))+'px';const t=drag.ghost.querySelector('.et');if(t)t.textContent=timeFromMins(m);drag.dateTag.textContent=`${dayLabel(drag.targetDate)}・${timeFromMins(m)}`}})();